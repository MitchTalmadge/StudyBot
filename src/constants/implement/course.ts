export namespace CourseImplementConstants {
  /**
   * Used to inform other implements of how many channels will 
   * be created for each course.
   */
  export const CHANNELS_PER_COURSE_IMPLEMENT = 2; 
}